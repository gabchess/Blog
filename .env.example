# Sanity project config (public, safe to expose in client bundles)
PUBLIC_SANITY_PROJECT_ID=2zl2a0h3
PUBLIC_SANITY_DATASET=production
PUBLIC_SANITY_API_VERSION=2024-01-01

# Sanity API token (server-side only, never prefix with PUBLIC_)
SANITY_API_READ_TOKEN=

# Mailgun mailing list (server-side only)
MAILGUN_API_KEY=
MAILGUN_LIST_ADDRESS=

# Vercel ISR cache bypass token (set in Vercel dashboard)
VERCEL_BYPASS_TOKEN=
