{
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ]; then echo 'Building main (Astro)'; exit 1; else echo 'Skipping non-main branch build (Q pivot 2026-05-26: this branch targets GCP/Docker, not Vercel)'; exit 0; fi"
}
