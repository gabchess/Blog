---
title: "Loom retro session close 2026-06-10 — vite-react-ssg confabulation cascade"
date: "2026-06-10T23:59:59Z"
session_id: "loom-blog-v2-feature-react-ssg-close"
source_harness: loom
agents_used: [bram, bobbin, vite-react-ssg-build, tsc]
failures_found: ["confabulation_main_mjs", "anosognosia_cleanup_crash", "locked_in_ssr_phase", "recurrent_temp_dir_poison"]
failure_classes: ["confabulation", "anosognosia", "locked_in"]
tags: [retro, loom, auto-generated, session-close, build-failure, ssr-broken, vite-react-ssg-unstable]
---

## Session Summary

**Branch:** feature/react-ssg
**Status:** INCOMPLETE — build pipeline blocked by vite-react-ssg SSG module generation failure
**Time spent:** Multiple build attempts (3+ retries), same failure pattern persists
**Commits landed:** 1 (e7ce8ab fix(build): ensure dist dirs exist)
**Untracked files:** 33 (build artifacts, retro documents, entry files)
**Root cause:** vite-react-ssg v0.9.0 does not generate `main.mjs` in temp directory; cascading cleanup failure poisons state

## Failure Cascade Anatomy

### Failure 1: Confabulation (main.mjs)

**Signal:** `ERR_MODULE_NOT_FOUND: Cannot find module '.../main.mjs'`

Plugin created temp directory `/Users/gava/projects/blog-v2/apps/web/.vite-react-ssg-temp/z45n62ln2c/` but never wrote `main.mjs` to it. During SSR phase, plugin attempted to import a module that does not exist.

**AR-381 class:** `confabulation` — hallucinated file path / expected file never created

### Failure 2: Anosognosia (cleanup crash)

**Signal:** `ENOTEMPTY: directory not empty, rmdir '.../.vite-react-ssg-temp'`

After SSR crashed, cleanup phase attempted `rmdir()` on a non-empty directory without first removing contents. No error handling; process terminated with poisoned temp state.

**AR-381 class:** `anosognosia` — failure to acknowledge prior error; attempted cleanup without preconditions met

### Failure 3: Locked-In (state corruption)

**Signal:** Client build claimed success + file copy; server build cannot find the file

File state became inconsistent between build phases. Client reported `dist/index.html` copied; SSR phase could not locate it.

**AR-381 class:** `locked_in` — state corruption mid-process (build claims success but artifacts missing)

## Build State at Session Close

- ✅ Client build: succeeds
- ✅ TypeScript check: succeeds
- ✅ Static HTML pages: 37/37 generated to `dist/posts/*/index.html`
- ✅ Static loader manifest: created
- ❌ SSR phase: crashes before `main.mjs` available
- ❌ Cleanup phase: crashes on non-empty temp directory
- ❌ Overall build: FAILED

**Key insight:** Static pages ARE being generated successfully. SSR output appears unused. The pipeline is over-engineering for this product.

## Recurrence Risk: HIGH

This is the **second vite-react-ssg confabulation failure in this session**. Prior: missing `Post-UX0GuF_e.js` asset file (also AR-381 confabulation). Pattern indicates:

1. vite-react-ssg v0.9.0 is fundamentally unstable for this monorepo structure
2. Module generation bug is triggered by specific config (Tailwind + React 19 + monorepo layout)
3. Temp directory poisoning blocks all subsequent builds until manually cleaned
4. Same failure will recur until underlying module generation bug is fixed

## Mitigation (Immediate)

```bash
rm -rf apps/web/.vite-react-ssg-temp
pnpm --filter @octant/web build
```

This unblocks the build IF the underlying module bug is transient. If it persists, the problem is structural.

## Structural Fixes Required

### 1. Defensive temp cleanup (SHORT-TERM)

Add to `build-setup.mjs` BEFORE running vite-react-ssg:

```javascript
import { rmSync } from 'fs';
const tempDir = new URL('.vite-react-ssg-temp', import.meta.url);
try {
  rmSync(tempDir, { recursive: true, force: true });
} catch (err) {
  console.warn('Could not clean temp dir:', err.message);
}
```

This prevents temp directory poison state from blocking future builds.

### 2. Evaluate SSR necessity (MEDIUM-TERM)

- All 37 static pages generated successfully
- Verify whether SSR output is actually required for product
- If SSR is not needed, disable in vite config or switch to simpler SSG approach
- If SSR IS required, evaluate alternative SSG tools

### 3. vite-react-ssg upgrade or replacement (LONG-TERM)

- v0.9.0 is early/unstable
- Check GitHub issues for known `main.mjs` / temp cleanup bugs
- Consider: Astro, Next.js static export, or maintained SSG alternative
- Current choice may not be right for this codebase

## Open Items (Priority Order)

### BLOCKING (before next build)
- [ ] `rm -rf apps/web/.vite-react-ssg-temp`
- [ ] Confirm build succeeds on clean state

### SHORT-TERM (Session N+1)
- [ ] Add defensive `rmSync` cleanup to `build-setup.mjs`
- [ ] Document SSR phase necessity vs static-only output
- [ ] If SSR not needed, disable it in vite.config.ts

### MEDIUM-TERM (Sprint)
- [ ] Investigate vite-react-ssg v0.9.0 module generation bug
- [ ] Evaluate SSG alternatives (Astro, Next.js static, Eleventy)
- [ ] Decision gate: upgrade plugin vs switch to alternative

## Cross-References

- **Temp poison dir:** `/Users/gava/projects/blog-v2/apps/web/.vite-react-ssg-temp/`
- **Build command:** `node build-setup.mjs && tsc --build && vite-react-ssg build`
- **Plugin:** vite-react-ssg v0.9.0
- **Branch:** feature/react-ssg
- **Commits:** e7ce8ab (only landing commit in this session)
- **Untracked state files:** 33 (see `git status`)
- **Related retros:** 
  - `.loom-retro-feedback-2026-06-10.md` (initial failure)
  - `.loom-retro-cascading-ssg-failures-2026-06-10.md` (cascade detail)
  - `.loom-retro-experiment-vite-react-ssg-integration-2026-06-10.md` (experiment proposal)

## Failure Pattern Summary (for Annie Track F)

**Recurrent confabulation** in vite-react-ssg SSR module generation across 2 build attempts in same session. Suggests structural incompatibility between plugin and codebase (monorepo structure, Tailwind, React 19, or specific import patterns trigger the bug). Not a one-off transient failure.

**Recommendation:** This pattern warrants an AR-381 recurrence diagnosis. If 2+ of the same failure class (confabulation) appear in the next 5 retro files for this branch, escalate to Mother for structural skill/rule fix proposal.

---

Session close generated: 2026-06-10T23:59:59Z
Failure-to-close signal: yes (build blocked)
Ready for next session: yes (open items clear, mitigation unblocked)
