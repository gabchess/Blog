---
title: "Build Session Retro — vite-react-ssg SSG Rendering Failure"
date: "2026-06-10T00:00:00Z"
session_id: "blog-v2-build-2026-06-10"
source_harness: "loom"
agents_used: []
failures_found: ["vite_react_ssg_path_error", "manifest_enoent", "path_concatenation_bug"]
failure_classes: ["confabulation"]
tags: [retro, build-failure, vite-react-ssg]
---

## Build Failure Summary

**Status:** FAILED  
**Phase:** SSG page rendering (37 pages)  
**Root Cause:** vite-react-ssg path concatenation bug

### Error Details

```
[vite-react-ssg] An internal error occurred.
[Error: ENOENT: no such file or directory, open '/Users/gava/projects/blog-v2/apps/web/dist/.vite/manifest.json']
```

Path duplication in error trace:
```
/Users/gava/projects/blog-v2/app/Users/gava/projects/blog-v2/apps/web:
```

The path `/Users/gava/projects/blog-v2/` is being concatenated twice, creating an invalid nested path.

## Failure Signals Detected

1. **Font resolution warnings** (non-critical at build time)
   - `/fonts/arcane-fable.woff` — will resolve at runtime
   - `/fonts/arcane-fable.woff2` — will resolve at runtime
   - `/fonts/arcane-fable.otf` — will resolve at runtime

2. **Manifest file not found** (critical)
   - vite-react-ssg expects `.vite/manifest.json` in the client build output
   - File path being incorrectly constructed with duplicate root path

3. **SSG rendering crash** (fatal)
   - Process terminates at page rendering step (37 pages queued)
   - Uncaught exception in vite-react-ssg internal flow

## AR-381 Classification

**Failure Class:** `confabulation`

The vite-react-ssg tool is constructing a hallucinated/incorrect file path by duplicating the project root directory. This is not a missing file (which would be `source_amnesia`), but a fabricated path from internal tool state.

## Open Items

1. **Primary**: vite-react-ssg SSG build phase fails with path concatenation bug
   - Affects: Cannot generate static pages for 37 routes
   - Impact: Blog blog build is broken; deploy blocked
   - Severity: CRITICAL

2. **Secondary**: Font asset references unresolved at build time
   - Status: Non-blocking (will resolve at runtime via CSS @font-face)
   - Impact: No functional issue; informational warnings only

## Cross-References

- Vite config: `/Users/gava/projects/blog-v2/apps/web/vite.config.ts`
- Build script: `pnpm --filter @octant/web build`
- vite-react-ssg version: Check `apps/web/package.json` dependencies
- Related issue: vite-react-ssg path handling in SSR + client manifest loading
