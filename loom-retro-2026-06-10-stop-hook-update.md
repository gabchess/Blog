---
title: "Loom retro 2026-06-10 stop-hook update"
date: "2026-06-10T23:59:59Z"
session_id: "blog-v2-react-ssg-vite-buildfix-final"
source_harness: loom
failure_caught_by: "stop-build-gate hook"
failure_class: "confabulation"
tags: [retro, loom, auto-generated, build-failure, vite-react-ssg, stop-hook-signal]
---

## Stop Hook Signal: Failure 4 — Vite SSR Manifest Missing

**Caught by:** `/Users/gava/projects/blog-v2/.claude/hooks/stop-build-gate.sh`  
**Time:** 2026-06-10 23:59:59Z (session-close attempt)  
**Command:** `pnpm --filter @octant/web build`

### Error Output

```
[vite-react-ssg] Rendering Pages... (37)

[vite-react-ssg] An internal error occurred.
[vite-react-ssg] Please report an issue, if none already exists: https://github.com/daydreamer-riri/vite-react-ssg/issues

[Error: ENOENT: no such file or directory, open '/Users/gava/projects/blog-v2/apps/web/dist/.vite/ssr-manifest.json'] {
  errno: -2,
  code: 'ENOENT',
  syscall: 'open',
  path: '/Users/gava/projects/blog-v2/apps/web/dist/.vite/ssr-manifest.json'
}

Node.js v25.8.1
```

### Failure Classification

**Failure class:** `confabulation`

**Why:** vite-react-ssg's rendering phase assumes `dist/.vite/ssr-manifest.json` exists. This manifest is typically generated by Vite during its build process. The build sequence does not invoke Vite in a way that generates this manifest before vite-react-ssg attempts to read it.

### Build Command Analysis

Current sequence:
```bash
node build-setup.mjs && tsc --build && vite-react-ssg build
```

**Problem:** No explicit `vite build` before `vite-react-ssg build`. The SSR manifest is a Vite-generated artifact, not a vite-react-ssg artifact. vite-react-ssg expects it to exist.

**Root cause:** Incomplete understanding of Vite build lifecycle. The Vite build generates:
- `dist/` client bundle
- `dist/.vite/manifest.json` (client manifest)
- `dist/.vite/ssr-manifest.json` (server-side rendering manifest)

vite-react-ssg's SSG rendering phase reads the SSR manifest to know which entry points exist and how to render them.

### Diagnosis Path

1. Check `apps/web/vite.config.ts` for SSR build configuration
2. Verify that Vite is configured to generate SSR entry points
3. Determine if `vite-react-ssg build` invokes Vite correctly, or if an explicit `vite build` step is needed
4. Check if there's a separate SSR build step that must run before SSG rendering

### Status

**Build gate:** BLOCKED ❌  
**Session-close:** PREVENTED by stop hook ✓ (working as intended)  
**Severity:** High — build will not complete until this is resolved

### Next Steps

1. Investigate vite.config.ts SSR/build configuration
2. Test explicit `vite build` before `vite-react-ssg build`
3. Review vite-react-ssg documentation for expected Vite setup
4. Update build command sequence if needed

### Cross-Reference

This failure supplements the main retro at:  
`/Users/gava/Obsidian/Arcana Wiki/00-Inbox/feedback/loom-retro-2026-06-10-react-ssg-final.md`

Related failures in sequence:
- Failure 1: Vite hook execution order (confabulation) — RESOLVED mid-session
- Failure 2: StaticDataLoader contract mismatch (source_amnesia) — ACTIVE
- Failure 3: Build state consistency gap (locked_in) — Dependent on 1 & 2
- **Failure 4: SSR manifest missing (confabulation) — ACTIVE at stop hook** ← NEW

---

**Signal captured:** 2026-06-10T23:59:59Z  
**Source:** stop-build-gate hook  
**Routing:** Annie inbox (see main retro file)
