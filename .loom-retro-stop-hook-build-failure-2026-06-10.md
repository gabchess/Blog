---
title: "Loom retro — vite-react-ssg SSR build failure"
date: "2026-06-10T00:00:00Z"
session_id: "loom-build-session-2026-06-10"
source_harness: loom
agents_used: []
failures_found: ["vite_react_ssg_file_not_found", "asset_hash_mismatch", "ssr_build_crash"]
failure_classes: ["confabulation"]
tags: [retro, loom, build-gate, stop-hook, auto-generated]
---

## Failure Summary

**vite-react-ssg** SSR build phase crashed with `ENOENT` when attempting to load an asset file that does not exist in the temp directory.

### Error Detail

```
Error: ENOENT: no such file or directory, open '/Users/gava/projects/blog-v2/apps/web/.vite-react-ssg-temp/9cqioafy76/assets/Post-UX0GuF_e.js'
```

### Root Cause (Confabulation Class)

The vite-react-ssg plugin expected to find `Post-UX0GuF_e.js` in the temp directory but it does not exist. The client build produced `Post-CocNmTi5.js` instead. This indicates:

1. **Asset filename hash mismatch** between client build and SSR build expectations
2. **Stale temp directory** state or cleanup issue between build phases
3. **Plugin bug** in vite-react-ssg: it either cached a wrong filename reference or failed to track the actual client asset names

### Build State at Failure

- ✅ Vite client build **succeeded** (1m 21s)
- ❌ Vite SSR build **failed** during asset loading
- ✅ Static HTML pages **were generated** (37 posts output to dist/posts/*/index.html)
- ❌ SSR bundle **never completed**

### Why This Is Confabulation (AR-381)

The plugin invented/hallucinated a file path (`Post-UX0GuF_e.js`) that was never produced by the build system. This is textbook confabulation: memory hole filled with plausible fiction. The plugin should have either:

1. Dynamically discovered the actual client asset filename (`Post-CocNmTi5.js`)
2. Used a stable reference mechanism (manifest file, loader data)
3. Registered its dependency on client assets before attempting to load them

Instead, it assumed a filename would exist without verifying.

## Open Items

1. **Immediate**: Investigate vite-react-ssg plugin behavior with client asset discovery
   - Does it cache filenames between build phases?
   - Does it parse the client bundle manifest before SSR phase?
   - Is there a race condition in temp directory access?

2. **Short-term**: Consider using a static loader data approach
   - Pre-generate asset → page mappings
   - Load from manifest instead of dynamic filename resolution

3. **Medium-term**: Evaluate vite-react-ssg stability for production
   - This plugin is in heavy development
   - Frequent hash-related issues suggest architectural fragility
   - May need fallback or alternative SSG approach

## Failure Signals

- vite-react-ssg internal error with confabulated file path
- Temp directory asset naming inconsistency
- SSR build crash halts deployment pipeline
- Client build succeeds, SSR fails (partial-success state)

## Cross-References

- Session state: `/Users/gava/projects/blog-v2/`
- Build command: `pnpm --filter @octant/web build` (runs `node build-setup.mjs && tsc --build && vite-react-ssg build`)
- Plugin: vite-react-ssg v6.x.x (pinned in package.json)
- Temp directory: `/Users/gava/projects/blog-v2/apps/web/.vite-react-ssg-temp/`
- Stop hook: `/Users/gava/projects/blog-v2/.claude/hooks/stop-build-gate.sh`
- Retro generated: 2026-06-10
- Source harness: loom
